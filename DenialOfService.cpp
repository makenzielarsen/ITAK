//
// Created by Makenzie Larsen on 4/14/17.
//

#include "DenialOfService.h"
#include <vector>

using namespace std;

void readIn() {

}

//ResultSet* DenialOfServiceAnalyzer::run(ifstream &inputStream) {
//    vector<string> attackers;
//    vector<string> possibleAttackers;
//    vector<string> attackPeriods;
//    vector<string> timeFrame;
//
//    ResultSet* resultSet = nullptr;
//
//    resultSet->resultSet.insert("Likely attackers", attackers);
//    resultSet->resultSet.insert("Possible Attackers", possibleAttackers);
//    resultSet->resultSet.insert("Attack Periods", attackPeriods);
//    resultSet->resultSet.insert("Timeframe", timeFrame);
//
//    configuration.configurationParameters.insert("Timeframe", timeFrame);
//    configuration.configurationParameters.insert("Likely Attack Message Count", likelyThreshold);
//    configuration.configurationParameters.insert("Possible Attack Message Count", possibleThreshold);
//
//    for (int i = 0; i < addressToSummary.size(); i++) {
//
//    }
//
//    return resultSet;
//}
